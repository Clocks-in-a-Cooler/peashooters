//sets up the various HTML elements of the ui
var wrapper = document.getElementById("wrapper");

var canvas = document.createElement("canvas");
canvas.width = 800; canvas.height = 600;

//that's it?

//oh...right.
window.onload = function() {
    load_menu(main_menu);
};
